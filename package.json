{
  "name": "4-testing-website",
  "private": true,
  "description": "Statische website van 4-testing. Alleen nodig om de tests te draaien.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\" && sh mcp/run-tests.sh",
    "prepare": "git config core.hooksPath .githooks"
  },
  "devDependencies": {
    "jsdom": "^30.1.0"
  }
}
